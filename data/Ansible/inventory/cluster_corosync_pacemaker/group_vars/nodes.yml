# Autogenerated list of node names
nodes_list: "{{ groups['nodes'] | map('extract', hostvars, 'inventory_hostname') }}"

# Name of cluster
cluster_name: test_cluster

# Password for "hacluster" user
hacluster_pass: test

# Dict of virtual ip addresses
virtual_ip_dict:
  1: 10.0.3.110

# List of systemd services
services_list:
  - nginx
